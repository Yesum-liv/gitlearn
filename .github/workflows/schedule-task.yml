name: Schedule-task
on:
  workflow_dispatch:
  schedule:
      - cron: "*/5 * * * *"
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: scheduled-print-msg
      run: echo "Scheduled task"
